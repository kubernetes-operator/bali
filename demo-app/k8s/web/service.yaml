# React NodePort 서비스
# 브라우저에서 직접 접근할 수 있도록 NodePort로 외부 노출
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: demo-app
spec:
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000       # http://localhost:30000 으로 접근
  type: NodePort
