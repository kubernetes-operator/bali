# PostgreSQL ClusterIP 서비스
# 클러스터 내부에서만 접근 가능 (API Pod → DB Pod)
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: demo-app
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
