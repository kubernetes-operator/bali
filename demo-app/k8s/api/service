# FastAPI ClusterIP 서비스
# WEB Pod에서 API Pod로 접근하기 위한 서비스
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: demo-app
spec:
  selector:
    app: api
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
