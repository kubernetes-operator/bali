# API 환경변수 ConfigMap
# DB 연결 정보 등 설정값을 K8s ConfigMap으로 관리한다
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: demo-app
data:
  DATABASE_URL: "postgresql://demouser:demopassword@postgres:5432/demoapp"
